[project]
name = "latte-roaster"
version = "0.1.0"

dependencies = [
    "torch",
    "transformers[torch]",
    "huggingface_hub",
    "safetensors",
    "python-dotenv",
    "pydantic",
    "pydantic-settings"
]
